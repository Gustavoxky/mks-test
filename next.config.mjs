/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['mks-sistemas.nyc3.digitaloceanspaces.com'], // Adicione o hostname da imagem aqui
  },
};

export default nextConfig;
